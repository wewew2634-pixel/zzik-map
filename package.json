{
  "name": "zzik-live",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "lint": "pnpm --filter web lint"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "playwright": "^1.56.1",
    "prisma": "^7.0.0"
  },
  "dependencies": {
    "@prisma/client": "^7.0.0"
  }
}
